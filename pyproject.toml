# Папки, которые не нужно проверять
exclude = [
    ".git",
    "__pycache__",
    ".venv",
    "migrations"
]


[tool.ruff.lint]
per-file-ignores = { 
    "tests/*.py" = ["ALL"],
    "**/alembic/**/*.py" = ["ALL"]  # 👈 Игнорируем все файлы в любых папках alembic
}